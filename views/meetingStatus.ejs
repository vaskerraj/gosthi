<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%- title %></title>
    <link rel="shortcut icon" href="favicon.ico">
    <link rel="stylesheet" href="/vendor/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="/vendor/fontawesome-free/css/all.min.css">
    <link rel="stylesheet" href="/vendor/fontawesome-free/css/v4-shims.min.css">
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>
    <%- include('partials/frontHeader')%>
    <div class="container mt-5">
        <div class="row align-items-center">
            <div class="col-md-4 col-sm-10 col-12 mx-auto mt-5 text-center">
                <div class="clearfix font18 fontb text-left border-bottom pl-2 pr-2 mt-4"style="font-weight: 500;">
                    <%= data.title %>
                </div>
                <div class="clearfix mt-3">
                    <span class="fa fa-spinner fa-spin mr-2"></span> Waiting for host to start meeting.
                </div>
                <div class="clearfix card pt-3 pb-3 mt-3">
                    <div class="row font18">
                        <div class="col-5 text-right">ID:</div>
                        <input type="hidden" id="rawMeetingId" value="<%= data.meeting_id %>">
                        <div class="col-7 text-left fontb" id="meetingId" style="font-weight: 500;"><%= data.meeting_id %></div>
                    </div>
                    <% if(data.type === 'schedule'){ %>
                    <div class="row mt-2">
                        <div class="col-5 text-right">Date:</div>
                        <div class="col-7 text-left"><%= data.meeting_date %></div>
                    </div>
                    <div class="row mt-2">
                        <div class="col-5 text-right">Time:</div>
                        <div class="col-7 text-left"><%= data.meeting_time %></div>
                    </div>
                    <div class="row mt-2">
                        <div class="col-5 text-right">Duration:</div>
                        <input type="hidden" id="rawMeetingDuration" value="<%= data.meeting_duration %>">
                        <div class="col-7 text-left" id="meetingDuration"></div>
                    </div>
                    <% } %>
                </div>
            </div>
            <a href='https://15.206.115.114/<%=data.title%>#userInfo.displayName="<%=meeter%>"' id="meetinglink" class="d-none"></a>
        </div>
    </div>
    <%-include('partials/frontJs')-%>
    <script src="/js/GS.Front.js"></script>
</body>
</html>